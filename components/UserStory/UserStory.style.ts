import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  story: {
    marginTop: 15,
    marginRight: 10,
    width: 80,
    gap: 5,
    alignItems: 'center',
  },

  imgText: {
    fontSize: 12,
    overflow: 'hidden',
    color: '#022150',
    fontFamily: 'Inter 18pt Black',
    fontWeight: 500,
  },
});
