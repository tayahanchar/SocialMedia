import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  story: {
    marginTop: 15,
    marginRight: 10,
    width: 60,
    height: 70,
    gap: 5,
    alignItems: 'center',
  },

  imgText: {
    fontSize: 14,
    overflow: 'hidden',
    color: '#022150',
    fontFamily: 'Inter 18pt Black',
    fontWeight: 500,
  },
});
