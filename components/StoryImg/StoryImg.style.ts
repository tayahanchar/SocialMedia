import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  img: {
    borderRadius: 50,
  },

  imgWrapper: {
    borderWidth: 1.5,
    borderColor: '#F35BAC',
    borderRadius: 50,
    padding: 3,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
